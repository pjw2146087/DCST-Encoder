【运行方式】
测试模型：
直接运行predict.py（该文件会加载fine_tune.py生成的模型文件medel.pt），对样本数据进行识别，生成识别后的数据。
注：本次样本数据为随机选取的个别示例样本，如需测试模型在不同数据集上的效果，可以参考以下步骤进行训练。

训练模型：

首先将数据集文件夹（论文中提到）和模型文件夹放置于同一个文件夹下
-dataset
-code

1.先运行pretrain.py进行预训练

2.再运行fine_tune.py进行微调（该文件会加载pretrain.py生成的pth，并生成模型文件model.pt）。

3.
（1）如需分割单个数据，运行predict.py（该文件会加载fine_tune.py生成的pth）
从数据集找个你想要的excel单个数据，然后新建文件夹将对应的excel和json放进去

注意，上述三个文件必须保持模型参数一致

受限于国家地理信息数据安全问题，如有需要，更多数据请联系农业农村部农机作业监测与大数据应用重点实验室获取。


![img.png](img.png)



【Running Method】
Test Model:
Run predict.py directly (this file will load the model file medel.pt generated by fine_tune.py) to identify the sample data and generate the identified data.

Note: The sample data in this test are randomly selected individual example samples. If you need to test the model's performance on different datasets, you can refer to the following steps for training.

Training the Model:

First, place the dataset folder (mentioned in the paper) and the model folder in the same folder:

-dataset

-code

1. Run pretrain.py for pre-training

2. Then run fine_tune.py for fine-tuning (this file will load the pth generated by pretrain.py and generate the model file model.pt).

3.

(1) If you need to split a single data point, run predict.py (this file will load the pth generated by fine_tune.py)

Find the single data point in the dataset that you want in Excel, and then create a new folder and put the corresponding Excel and JSON files in it.

Note that the model parameters of the above three files must be consistent.

Due to national data security concerns, please contact the Key Laboratory of Agricultural Machinery Monitoring and Big Data Application, Ministry of Agriculture and Rural Affairs for more data.






